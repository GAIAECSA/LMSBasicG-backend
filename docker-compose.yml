version: "3.9"

services:
  api:
    build: .
    container_name: fastapi_app_basic_lms
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
    env_file:
      - .env
    restart: always